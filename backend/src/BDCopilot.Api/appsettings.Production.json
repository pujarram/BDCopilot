{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },

  "Ai": {
    "Provider": "AzureOpenAI",
    "AzureOpenAI": {
      "Endpoint": "https://bd-copilot.openai.azure.com",
      "ChatDeployment": "gpt-4o",
      "EmbeddingDeployment": "text-embedding-3-large",
      "UseEntraIdAuth": true
    }
  },

  "_comment": "ConnectionStrings:Postgres, BlobStorage:ConnectionString and Graph:* should come from Azure Key Vault / App Service configuration in production, not this file — this appsettings.Production.json intentionally only overrides the non-secret Ai section."
}
